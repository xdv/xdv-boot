// XDV-Boot: Paging Tests

forge BootPagingTests {

    proc K::test_page_size() {
        let size = 4096;
        return 0;
    }

    proc K::test_page_shift() {
        let shift = 12;
        return 0;
    }

    proc K::test_pte_present() {
        let pte = 1;
        return 0;
    }

    proc K::test_pte_writable() {
        let pte = 2;
        return 0;
    }

    proc K::test_pte_user() {
        let pte = 4;
        return 0;
    }

    proc K::test_paging_enable() {
        let result = 0;
        return 0;
    }
}
